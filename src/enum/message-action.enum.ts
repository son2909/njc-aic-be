export enum MessageActionEnum {
  CONNECT = '$connect',
  DISCONNECT = '$disconnect',
  DEFAULT = '$default',
  NOTIFICATION = 'notification',
  GROUP = 'group',
  FRIEND = 'friend',
  PING = 'ping',
  BROADCAST = 'broadcast',
}
